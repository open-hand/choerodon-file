spring:
  cloud:
    bus:
      enabled: false
  sleuth:
    stream:
      enabled: false
  stream:
    queue:
      enabled: false
  kafka:
    enabled: false

data:
  dir: src/main/resources
eureka:
  client:
    enabled: false

hystrix:
  stream:
    queue:
      enabled: false